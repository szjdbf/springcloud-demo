server:
  port: 8765

spring:
  application:
    name: serivce-feign

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

feign:
  hystrix:
    enabled: true #开启feign的断路器