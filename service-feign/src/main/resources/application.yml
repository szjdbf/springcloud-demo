server:
  port: 8765

spring:
  application:
    name: service-feign

eureka:
  client:
    service-url:
      defaultZone: http://root:123456@localhost:8761/eureka/

feign:
  hystrix:
    enabled: true #开启feign的断路器