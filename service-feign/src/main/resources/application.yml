server:
  port: 8765

spring:
  application:
    name: service-feign

eureka:
  client:
    service-url:
      defaultZone: http://root:123456@localhost:8761/eureka/

feign:
  hystrix:
    enabled: true #默认是关闭的，需要手动开启开启feign的断路器